# logician

Lightweight Python MVP for an Autonomous DevOps Assistant (RAG + Qdrant).

This scaffold provides a minimal CLI, an embeddings wrapper, a Qdrant client wrapper (stub), a RAG orchestrator, and a basic unit test to verify imports.

See docs in the package for usage.
